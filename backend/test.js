const { Client } = require('pg'); const client = new Client({ connectionString: 'postgresql://iptvdb:SecureDbPass9aF2xL8qP3@35.154.128.217:5432/iptv_db' }); client.connect().then(() => client.query(SELECT * FROM app_settings WHERE setting_key IN ('upi_id', 'support_whatsapp', 'whatsapp_admin_number'))).then(res => { console.log(res.rows); return client.end(); }).catch(console.error);
