-- Migration 042: Placeholder
-- This file exists to preserve the sequential numbering of migrations.
-- Migration 042 was never created; this is a safe no-op placeholder.
-- If a migration 042 was ever applied on any environment, it was a one-off
-- script and is not reproducible here. Fresh installs are unaffected.
SELECT 1;
